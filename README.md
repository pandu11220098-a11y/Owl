# Owl
Owl
# Initialize a local git repository inside your project folder
git init

# Connect your local folder to your remote GitHub repository
git remote add origin https://github.com/your-username/my-first-project.git

# Stage all files, commit them, and push to main
git add .
git commit -m "Initial commit"
git branch -M main
git push -u origin main
